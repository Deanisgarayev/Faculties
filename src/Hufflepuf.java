public class Hufflepuf extends Hogwarts {
    public Hufflepuf(String name, String characteristic, int firstScore, int secondScore, int thirdScore) {
        super(name, characteristic, firstScore, secondScore, thirdScore);
    }
}
